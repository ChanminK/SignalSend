Signal Send - send signals & recieve them
Use a server or smth to do this stuff??

Public "void" or area where people can both put in encrypted signals and also find random ones as well

Server stores ciphertext + metadata

Server:

Backend:
    Flash + SQLite

Frontend:
    Html + Js

Encrypt: (GPT SHOWED ME HOW TO DO THIS)
    KDF: PBKDF2 (portable) with random 16-byte salt.
    AEAD: AES-GCM with 12-byte iv.


HAVE ENCRYPT/DECRYT BE CLIENT SIDE THING

Classic  cipher (shift all letters by 1 or smth)
    RESAERCH OTHER TYPES AS well
AES-GCM Broadcast mode
Asymmetric (aka keypair) mode 

CURRENT GOAL:
    GET THE SEVER WORKING AND DONE

STUFF LEFT NOW: 

Optional polish (quick wins)
    Latest feed (if not already pasted).
    Hidden tags: client stores `tag_hash = SHA-256(tag)` and filters by hash; stop exposing raw tag.
    Auto cleanup expired rows (call a `cleanup_expired()` helper on each GET or via a tiny cron script).
    Unit tests for crypto round-trips and API contracts (pytest).

STRETCH GOALS:
    X25519-AES asymmetric mode (ephemeral ECDH + HKDF + AES-GCM).
    Rate limit persistence** (Redis) + IP allowlist for demo hosts.
    PostgreSQL + proper indices.
    Attachments: client-side encrypted blobs stored in S3/minio.
    Admin/report endpoints and a small admin page.


WHY DOESNT REACT WANNA COOPERATE MAN
Y'know what fine vercel full it is